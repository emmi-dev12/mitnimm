# Public site URL (Render sets RENDER_EXTERNAL_URL; override if you have a domain)
APP_URL=http://localhost:5173

# BotFather token — never commit the real value
TELEGRAM_BOT_TOKEN=

# Optional: one chat for “new pile” posts
TELEGRAM_CHAT_ID=
